{{- if .Values.rbac.create }}

apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: {{ include "employee-api.name" . }}-role
  namespace: {{ .Values.namespace.name }}

rules:
- apiGroups: [""]
  resources:
    - pods
  verbs:
    - get
    - list
{{ end }}